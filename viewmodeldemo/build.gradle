plugins {
    id 'com.android.application'
}
apply from: '../dependencies.gradle'

android {
    defaultConfig {
        applicationId "com.cainiaowo.viewmodeldemo"
    }
}

dependencies {

    // 存在实现ViewModel懒加载的方式
    implementation 'androidx.activity:activity-ktx:1.2.0'
}